---
import { cn } from '@/lib/utils/cn';
import { SectionHeader } from '@/components/ui/section-header';
import type { HTMLAttributes } from 'astro/types';

export interface Props extends HTMLAttributes<'section'> {
    eyebrow?: string;
    heading?: string;
    class?: string;
}

const {
    eyebrow = 'How Travelity solves it',
    heading = 'Each problem, addressed directly.',
    class: className,
    ...rest
} = Astro.props;
---

<section
    class={cn('solution-map-section bg-paper py-16 md:py-24', className)}
    {...rest}
>
    <div class="container mx-auto px-6">
        <SectionHeader eyebrow={eyebrow} eyebrowWithRule align="center">
            <Fragment slot="headline">{heading}</Fragment>
        </SectionHeader>
        <div class="grid grid-cols-1 gap-4 max-w-[960px] mx-auto">
            <slot />
        </div>
    </div>
</section>
