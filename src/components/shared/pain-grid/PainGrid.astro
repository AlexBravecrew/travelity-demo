---
import { cn } from '@/lib/utils/cn';
import { SectionHeader } from '@/components/ui/section-header';
import type { HTMLAttributes } from 'astro/types';

export interface Props extends HTMLAttributes<'section'> {
    eyebrow?: string;
    heading?: string;
    class?: string;
}

const {
    eyebrow = 'The problem you face',
    heading = 'Three places the day breaks down.',
    class: className,
    ...rest
} = Astro.props;
---

<section
    class={cn('pain-grid-section bg-surface-alt py-16 md:py-24', className)}
    {...rest}
>
    <div class="container mx-auto px-6">
        <SectionHeader eyebrow={eyebrow} eyebrowWithRule align="center">
            <Fragment slot="headline">{heading}</Fragment>
        </SectionHeader>
        <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
            <slot />
        </div>
    </div>
</section>
